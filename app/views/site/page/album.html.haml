-@range.each do |image|
  =link_to image_tag("galeria/#{params[:page_num]}/img#{image}_p.jpg"), asset_path("galeria/#{params[:page_num]}/img#{image}.jpg"), class: "fancybox", :data => {:"fancybox-group" => "album"}
.clearboth