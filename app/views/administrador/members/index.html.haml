%h1
  Listando Membros

%table
  %tr
    %th
      Nome
    %th
      Vinculado à Familia
    %th
    %th
    %th
  -@membros.each do |member|
    %tr
      %td
        = member.name
      %td
        = member.family.name
      %td
        = link_to 'Mostrar', administrador_member_path(member.id), method: :get
      %td
        = link_to 'Editar', edit_administrador_member_path(member)
      %td
        = link_to 'Deletar', administrador_member_path(member), method: :delete, data: { confirm: "Esta certo que quer deletar o member #{member.name} ?" }
%br
= link_to 'Novo Membro', new_administrador_member_path
