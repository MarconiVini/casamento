%h1
	Listando Acompanhantes
-if @follower.length > 0
	%table
		%tr
			%th
				Nome
			%th
				Vinculado à Familia
			%th
				Descrição
			%th
				Data de Criação
			%th
				Deletar?
		-@follower.each do |f|
			%tr
				%td
					=f.name
				%td
					=f.family_name
				%td
					=f.description  
				%td
					%span{title: "#{l f.created_at}"}
						= distance_of_time_in_words(f.created_at, Time.now, true)   
				%td
					= button_to 'Deletar', administrador_acompanhante_path(f), method: :delete, data: { confirm: "Esta certo que quer deletar o Acompanhante #{f.name} ?" }
-else
	Não há nenhum registro					
